---
title: "Quarto Manuals Explained"
subtitle: "A guide to creating and using Quarto Manuals"
---

## Abstract

A Quarto Manual is a structured, multi-document Quarto template. It contains ordered, 
executable `.qmd` pages, workflow-specific helper libraries, page-level tests, and a final 
readiness checklist. Organizations, research groups, and individuals can create their own Quarto Manuals
to document and share complex, procedural computing workflows. 

Manuals are common in many
workplaces and every day life:

- Flight check manuals for pilots and cabin passengers
- HR training websites with modules and quizzes
- Cleaning and maintenance procedures for hotels and restaurants
- Standard operating procedures for experimental participants and laboratory technicians

Manuals are used in the workplace to ensure that critical workplace procedures are followed correctly 
and consistently, and — perhaps most importantly — rely on active participation from the user 
and frequent spot checks to ensure that the workflows are being followed correctly.

Working with software need not be any different.

> Quarto Manuals are interactive, executable software manuals for setting up and operating 
> research computing projects. They are designed to be used by researchers, students, and 
> anyone who might need to follow a complex, multi-step computing workflow in a consistent and reproducible manner.


## Basic use

There are naturally two audiences for Quarto Manuals: writers, 
and end-users. Writers create workflow manuals for others to follow, and 
end-users follow those manuals to set up and operate the workflow.

> **You**: I want to download ERA5 data from the CDS API, and I have
an example Python script, but I don't know how to set up SLURM for
multiple jobs, Python virtual environments for different CDS API Downloader projects, or Snakemake for pipeline development.

As a manual end-user, you might be looking to
have a structured, reproducible set up for your 
computing workflows, but might be overwhelmed by the complexity of 
a setup process that spans multiple tools, domains of knowledge, 
and skill sets.
Like buying IKEA furniture and following the assembly instructions, you
know what you want to achieve, but you don't necessarily know
how to do the whole process from scratch. So you follow the
provided step-by-step manual to achieve your goal.

To use an existing manual, fetch the template from GitHub:

```bash
quarto use template GoldenPlanetaryHealthGroup/ERA5-Download-Manual
```

Here, `GoldenPlanetaryHealthGroup/ERA5-Download-Manual` is the GitHub repository for the manual you want to use.

Quarto will create a new project from that template. The generated project contains a set of `.qmd` pages that step you through the setup process. You can read and execute the pages in order.

A typical manual page follows this pattern:

1. discover the current project state;
2. explain the setup step;
3. ask the user for any project-specific values;
4. generate or modify setup files;
5. run a test or inspection;
6. update the final readiness checklist.

> **You**: I keep having to look up how to set up my Python 
virtual environments for different CDS API Downloader projects, 
and writing a single CLI for it would mean writing a CLI that is too specific to be useful.

As a manual author, you might want to encapsulate a logical workflow
into a set of steps that is flexible enough to be used by
other people and projects, but still reproducible and robust enough to
ensure you don't have to look up or copy-paste the same steps
over and over again. This is a great scenario for an operator's
manual.

To create a manual for your own workflow, start with a Quarto project and use the `quarto-manual` project type or extension in `_quarto.yml`:

```yaml
project:
  type: quarto-manual
```

Then create one `.qmd` page for each setup step in your workflow.

Each page should contain:

- a short explanation of what the step does;
- a code chunk that discovers the current project state;
- any user-editable variables for that step;
- code or export blocks that create the required files;
- an inspection or test that proves the step is complete.

For example:

````markdown
# 02 Request Specification

## Start from current project state

```{python}
from quarto_manual.project import Project

project = Project.discover()
```

## Assemble

Create the request specification for this project.

```{python}
print(
    """
    Some python code that does some stuff to the project environment, for example making directories,
    """
)
```
## Inspect

Run the page-level check that confirms the request specification exists and is valid.

```{python}
from quarto_manual.project.page_02 import test_request_specification

test_request_specification(project)
```
````

By opening each page and running the code chunks in order,
you will be guided through the setup process for the workflow, and 
you will have a complete set of configuration files, source code, 
and tests for your project, including code-backed customizations that 
_you_ can make specific to your project.

## Export blocks

Manual pages can include special blocks that export files into the project.

For example:

````markdown
::: {.manual-project-source file="src/{{ project_package }}/request.py"}
```python
def build_request():
    return {}
```
:::
````

This block says:

> When the manual is exported, write this code to `src/{{ project_package }}/request.py`.

Common block types include:

- `.manual-lib-source` for reusable helper code;
- `.manual-project-source` for project source files (Python, R, Julia etc.);
- `.manual-project-test` for tests;
- `.manual-project-script` for shell or Slurm scripts;
- `.manual-project-config` for YAML, TOML, Markdown, or other configuration files;
- `.manual-run` for code that runs in the page but is not exported;
- `.manual-explain` for explanatory callouts.

## What a manual produces

A manual produces setup evidence, not necessarily final research outputs.

Depending on the workflow, that evidence might include:

- configuration files;
- source modules;
- helper scripts;
- Slurm scripts;
- package files;
- container recipes;
- tests;
- logs;
- a completed readiness checklist.

Much like a pre-flight checklist, a manual should answer:

> Is this project ready to operate?

The user still runs the actual workflow.

## Comparisons

### Why not just write a really really good README?

### Ok, then why not just write a really really good tutorial website?

quarto-manual vs software carpentries, Jupyter Book, plain Quarto, learnr, nbgrader, marimo, binder