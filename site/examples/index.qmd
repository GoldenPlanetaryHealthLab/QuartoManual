---
title: "Quarto Manuals Explained"
subtitle: "How Executable Operations Manuals Are Built From Independent Quarto Pages"
format:
  html:
    toc: true
    code-fold: false
author: Tinashe M. Tapera
date: last-modified
filters:
  - _extensions/quarto-manual/manual.lua
execute: 
  eval: false
---

## What Is A Quarto Manual?

A Quarto Manual is an executable operations manual for a software or research
workflow.

Use a Quarto Manual when a workflow is too long, fragile, or context-specific
for a README, but too visible and judgment-heavy to hide behind a single CLI.

A manual gives the user ordered `.qmd` pages that:

- explain one step at a time;
- run the code for that step;
- verify whether the step worked;
- make dependencies between steps explicit.

The manual does not need to own a shared runtime object model. Instead, each
page owns its own state and proves its own result.

## Core Idea

The central design rule is simple:

> A Quarto Manual is an ordered set of independent executable pages.

Each page is a contract with three parts:

1. `prereq`: prove that the page is allowed to run;
2. `procedure`: do one or more pieces of work;
3. `check`: prove that the work succeeded.

This avoids hidden state between pages.

If page 3 depends on page 2, page 3 should enforce that dependency in its
prerequisite block by checking visible project state:

- required files exist;
- required directories exist;
- a previous test suite passes;
- a required tool is installed;
- a prior configuration file contains the expected values.

Pages therefore share a project, not a notebook kernel.

## Why This Shape?

This structure is useful because it keeps responsibility local.

- The page owns its own setup conditions.
- The page owns the work it performs.
- The page owns the test that proves the result.

This is close to familiar software design patterns:

- Design by contract:
  `prereq` acts like a precondition and `check` acts like a postcondition.
- Arrange / Act / Assert:
  `prereq` arranges the required state, `procedure` acts, and `check`
  asserts the result.
- Idempotent infrastructure steps:
  inspect current state, apply a change, then verify the target state.

That makes manuals easier to write, rerun, and debug.

## Page Contract

Every manual page should contain:

- exactly one `.manual-prereq` block;
- one or more `.manual-procedure` blocks;
- exactly one `.manual-check` block.

The Quarto Manual filter can validate that structure and render those blocks as
semantic callouts.

### Prerequisite

Use the prerequisite block to answer:

> What must already be true before this page can proceed?

Typical prerequisite checks include:

- `assert Path("README.md").exists()`
- `test -d data`
- `pytest tests/test_previous_step.py`
- `stopifnot(file.exists("config.yml"))`

### Procedure

Use one or more procedure blocks to answer:

> What should the user do on this page?

Procedure blocks are where the page creates files, edits configuration,
prepares data, runs commands, or materializes tests.

### Check

Use the check block to answer:

> How do we know this page worked?

A check should test the concrete artifacts created by the procedure blocks
above.

For example, a check might:

- assert that a config file exists;
- validate JSON or YAML contents;
- import an emitted helper function;
- run `pytest`, `testthat`, or `unittest`;
- verify that a shell script was written and is executable.

## Minimal Example

Here is the smallest useful page shape:

````markdown
---
title: "Create Configuration"
manual:
  page: 2
---

::: {.manual-prereq}
```{python}
from pathlib import Path

assert Path("README.md").exists()
assert Path("data").is_dir()
```
:::

::: {.manual-procedure}
```{python}
from pathlib import Path
import json

Path("config").mkdir(exist_ok=True)
Path("config/manual.json").write_text(
    json.dumps({"status": "configured"}, indent=2) + "\n",
    encoding="utf-8",
)
```
:::

::: {.manual-check}
```{python}
from pathlib import Path
import json

config = Path("config/manual.json")
assert config.exists()
assert json.loads(config.read_text())["status"] == "configured"
```
:::
````

See [page.qmd](page.qmd) for a fuller authoring template.

## Relationship To `quarto-emit`

Quarto Manual and `quarto-emit` have different jobs.

- Quarto Manual defines the structure of an executable manual page.
- `quarto-emit` optionally materializes reusable files from those pages.

You do not need `quarto-emit` to write a manual.

Use `quarto-emit` when a page should produce conventional project artifacts
such as:

- `src/` modules
- `tests/` suites
- `scripts/`
- `config/` files

For example, a check block may also emit a real pytest file:

````markdown
::: {.manual-check .emit-test-py file="tests/test_manual_page.py"}
```{python}
from pathlib import Path


def test_manual_page():
    assert Path("config/manual.json").exists()
```
:::
````

That keeps the page readable while still allowing the manual to materialize
files for later reuse.

## Author Workflow

To create a manual for your own procedure:

1. Start from the Quarto Manual template.
2. Write one page per operational step.
3. Give each page one prerequisite block, one or more procedure blocks, and
   one check block.
4. Keep page state local and enforce dependencies through prerequisites.
5. Use `quarto-emit` only when a page needs to materialize reusable files.

## Operator Workflow

To use an existing manual:

1. Find the repository for the procedure manual.
2. Run `quarto use template <ORG>/<PROCEDURE>_manual`.
3. Open the manual locally.
4. Work through the pages in order.
5. Let each page's prerequisite and check blocks tell you whether you can
   proceed.

## Scope Of This Project

This project is intentionally narrow.

Quarto Manual should only extend Quarto in one functional way:

- give manual pages semantic structure with `prereq`, `procedure`, and `check`
  blocks.

Everything else should be optional:

- emitted source files
- emitted test suites
- helper modules
- workflow-specific libraries

Those are choices a manual author may make, not requirements of the framework.
